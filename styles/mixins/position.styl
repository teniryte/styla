fixed($top = null, $right = null, $bottom = null, $left = null)
  position: fixed;
  if $top != null
    top: $top;
  if $right != null
    right: $right;
  if $bottom != null
    bottom: $bottom;
  if $left != null
    left: $left;
